package game.bin;

/**
 * Simple enumeration that contains the two levels of the game
 * for pawns displacements. In the easy level, the player only has to
 * specify the direction in which he wants to move his pawn. In the
 * hard level, he has to indicate the precise line and column.
 */
public enum Level {
    EASY,
    HARD
}
